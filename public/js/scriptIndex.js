const btnOffers = document.querySelector(".btn-input");

function viewOffers() {
//  btnOffers.textContent = `VER OFERTAS(0)`;
}

viewOffers();

//Destacando a nav Home

const activeNav = document.querySelectorAll(".nav-link")[0];
activeNav.style.filter = " brightness(100%)";

